Login and Registration

NodeJS and ExpressJS for the server, serving EJS files.
Connects to MongoDB on your localhost. Need to have MongoDB installed or else connect to a sandbox online.

In root folder open terminal and run "npm install" to download all dependencies.
Then run "npm server.js".

-Enjoy!